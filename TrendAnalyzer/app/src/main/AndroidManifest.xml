<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="vorotilenko.trendanalyzer" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrendAnalyzer" >
        <activity android:name=".activities.ChartActivity"/>
        <activity
            android:name=".activities.select.currency.SelectSecondCurrencyActivity"
            android:exported="true" />
        <activity android:name=".activities.select.currency.SelectFirstCurrencyActivity" />
        <activity android:name=".activities.select.exchange.SelectExchangeActivity" />
        <activity android:name=".activities.observedsymbols.ObservedSymbolsActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:noHistory="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>